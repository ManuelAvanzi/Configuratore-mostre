import {chromium} from '@playwright/test';
const browser=await chromium.launch({channel:'msedge',headless:true});const page=await browser.newPage({viewport:{width:1440,height:1000}});const errors=[],external=[];page.on('pageerror',e=>errors.push(e.message));await page.route('**/*',route=>{if(!route.request().url().startsWith('http://localhost:4173')&&!route.request().url().startsWith('data:')){external.push(route.request().url());return route.abort();}return route.continue();});await page.goto('http://localhost:4173/studio');await page.locator('[data-action="demo"]').click();await page.waitForTimeout(700);await page.screenshot({path:'test-results/finale-editor.png'});await page.locator('.top-actions [data-action="visit"]').click();await page.waitForTimeout(300);await page.screenshot({path:'test-results/finale-visita.png'});await browser.close();if(errors.length||external.length)throw Error(JSON.stringify({errors,external}));console.log('Build di produzione: editor e visita funzionanti, nessuna dipendenza di rete esterna.');

