import {materials} from './surfaces.js';
let category='Tutti';
export function materialLibrary(s){return `<label class="text-field material-filter">Libreria · ${materials.length} materiali<select id=material-category aria-label="Categoria materiali">${['Tutti',...new Set(materials.map(m=>m.category))].map(c=>`<option ${c===category?'selected':''}>${c}</option>`).join('')}</select></label><div class=material-library>${materials.map(m=>`<button data-material="${m.id}" data-material-category="${m.category}" ${category!=='Tutti'&&category!==m.category?'hidden':''} aria-pressed="${s.material===m.id}" class="material-card ${s.material===m.id?'active':''}">${m.asset?`<img class=material-sample src="/materials/${m.asset}/preview.webp" alt="" loading=lazy/>`:`<span class=material-sample style="background:${m.color}"></span>`}<span>${m.name}</span>${m.asset?'<small>2K</small>':''}</button>`).join('')}</div>`;}
export function setMaterialCategory(value){category=value;document.querySelectorAll('[data-material-category]').forEach(b=>b.hidden=category!=='Tutti'&&b.dataset.materialCategory!==category);}
